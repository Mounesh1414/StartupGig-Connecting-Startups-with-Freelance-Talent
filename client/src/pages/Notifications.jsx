const Notifications = () => {
  return (
    <section className="panel">
      <h1>Notifications</h1>
      <p>Notifications feed for applications, shortlisting, interviews, and payment updates.</p>
    </section>
  );
};

export default Notifications;
